Program Uno(Input,Output);
Var
        X,Y : Integer;

Procedure GCD(A,B : Integer);
Begin
   if b = 0 then GCD(a,b)
   else gcd(b, a mod b)
End;

Function GCD2(A,B : Integer) : Integer;
Begin
   if b = 0 then gcd2 := GCD2(a,b)
   else gcd2 := gcd2(b, a mod b)
End;


Begin
   GCD(X,Y)
End.
